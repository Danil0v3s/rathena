name: Analyse servers with CodeQL
# analysis_codeql.yml

concurrency:
  group: ${{ github.repository }}-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/master' }}

on:
  workflow_dispatch:
  merge_group:
  push:
    branches:
      - master
  pull_request:

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      should_run: ${{ steps.evaluate.outputs.should_run }}

    steps:
      - name: Checkout repository
        if: ${{ github.event_name == 'pull_request' }}
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Detect relevant changes
        if: ${{ github.event_name == 'pull_request' }}
        id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            codeql:
              - '.github/**'
              - 'tools/ci/**'
              - 'src/**'
              - '**/CMakeLists.txt'
              - 'CMakePresets.json'
              - 'vcpkg.json'

      - name: Decide whether CodeQL should run
        id: evaluate
        run: |
          if [ "${{ github.event_name }}" != "pull_request" ]; then
            echo "should_run=true" >> "$GITHUB_OUTPUT"
          elif [ "${{ steps.filter.outputs.codeql }}" = "true" ]; then
            echo "should_run=true" >> "$GITHUB_OUTPUT"
          else
            echo "should_run=false" >> "$GITHUB_OUTPUT"
          fi

  analyze:
    needs: changes
    # Github Actions checks for '[ci skip]', '[skip ci]', '[no ci]', '[skip actions]', or '[actions skip]' but not a hyphenated version.
    # It's a catch-all incase a Pull Request has been opened and someone is on auto-pilot.
    if: ${{ needs.changes.outputs.should_run == 'true' && !contains(github.event.head_commit.message, 'ci-skip') }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
          os: [ubuntu-latest]
          # We run build checks for both Renewal and PRE-Renewal
          mode: [PRE, RE]
          include:
            - mode: PRE
              prere: 'ON'
            - mode: RE
              prere: 'OFF'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Initializes the CodeQL tools for scanning.
      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
           # CodeQL supports [ 'cpp', 'csharp', 'go', 'java', 'javascript', 'python', 'ruby' ]
           # Learn more about CodeQL language support at https://aka.ms/codeql-docs/language-support
           languages: cpp
           config-file: tools/ci/codeql_config.yml

      # CodeQL traces the compiler, so the build has to run after init.
      - uses: ./.github/actions/cmake-build
        with:
          cache-key: gcc
          cmake-args: >-
            -DENABLE_PRERE=${{ matrix.prere }}
            -DENABLE_EXTRA_BUILDBOT_CODE=ON
          targets: login-server char-server map-server web-server

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3

  codeql-required:
    name: CodeQL required
    needs:
      - changes
      - analyze
    if: ${{ always() }}
    runs-on: ubuntu-latest

    steps:
      - name: Enforce required CodeQL result
        run: |
          if [ "${{ needs.changes.outputs.should_run }}" != "true" ]; then
            echo "No relevant source or CI changes detected; skipping CodeQL."
            exit 0
          fi

          if [ "${{ needs.analyze.result }}" = "success" ]; then
            echo "CodeQL analysis completed successfully."
            exit 0
          fi

          echo "CodeQL analysis did not complete successfully: ${{ needs.analyze.result }}"
          exit 1
